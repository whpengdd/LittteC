# LLM Configuration
# Choose one of the following based on your provider
OPENAI_API_KEY=
GEMINI_API_KEY=
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=

# Service Configuration
# Optional: Set host and port if needed (defaults usually handled in code)
HOST=0.0.0.0
PORT=8000
